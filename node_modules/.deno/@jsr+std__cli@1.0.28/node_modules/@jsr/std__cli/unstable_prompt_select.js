// Copyright 2018-2026 the Deno authors. MIT license.
import { handlePromptSelect } from "./_prompt_select.js";
const ETX = "\x03";
const ARROW_UP = "\u001B[A";
const ARROW_DOWN = "\u001B[B";
const CR = "\r";
const DELETE = "\u007F";
/**
 * Shows the given message and waits for the user's input. Returns the user's selected value as string.
 *
 * Also supports filtering of the options by typing.
 *
 * @typeParam V The value of the underlying Entry, if any.
 * @param message The prompt message to show to the user.
 * @param values The values for the prompt.
 * @param options The options for the prompt.
 * @returns The string that was entered or `null` if stdin is not a TTY.
 *
 * @example Basic usage
 * ```ts ignore
 * import { promptSelect } from "@std/cli/unstable-prompt-select";
 *
 * const browser = promptSelect("Please select browser", [
 *   "Chrome",
 *   "Firefox",
 *   "Safari",
 * ], { clear: true });
 * ```
 *
 * @example With title and value
 * ```ts ignore
 * import { promptSelect } from "@std/cli/unstable-prompt-select";
 *
 * const browsers = promptSelect(
 *   "Please select browsers:",
 *   [{
 *     label: "safari",
 *     value: 1,
 *   }, {
 *     label: "chrome",
 *     value: 2,
 *   }, {
 *     label: "firefox",
 *     value: 3,
 *   }],
 *   { clear: true },
 * );
 * ```
 *
 * @example With multiple options
 * ```ts ignore
 * import { promptSelect } from "@std/cli/unstable-prompt-select";
 *
 * const browser = promptSelect("What country are you from?", [
 *   "Brazil",
 *   "United States",
 *   "Japan",
 *   "China",
 *   "Canada",
 *   "Spain",
 * ], { clear: true, visibleLines: 3, indicator: "*" });
 * ```
 */ export function promptSelect(message, values, options = {}) {
  if (!Deno.stdin.isTerminal()) return null;
  let selectedIndex = 0;
  handlePromptSelect(message, options.indicator ?? "❯", values, options.clear, options.visibleLines, options.fitToRemainingHeight, (active, absoluteIndex)=>{
    if (active) {
      selectedIndex = absoluteIndex;
    }
  }, (str, _absoluteIndex, { etx, up, down, remove, inputStr })=>{
    switch(str){
      case ETX:
        return etx();
      case ARROW_UP:
        up();
        break;
      case ARROW_DOWN:
        down();
        break;
      case CR:
      case " ":
        return true;
      case DELETE:
        remove();
        break;
      default:
        inputStr();
        break;
    }
    return false;
  });
  return values[selectedIndex] ?? null;
}
//# sourceMappingURL=unstable_prompt_select.js.map