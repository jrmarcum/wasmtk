# cmem — Portable Project Memory for wasmtk

This folder is the **authoritative, portable project memory** for `wasmtk`. It lives inside
the project tree, so it travels with the project on the USB drive (and is committed to git,
unlike the legacy `CLAUDE.md`, which is `.gitignore`d and therefore machine-local only).

**Format:** plain Markdown — one focused topic file per domain, so any single concern can be
reviewed and revised without wading through one giant file. Keep files small and single-topic.

## Policy (durable — set by the project owner 2026-05-31)

- **`cmem/` is the single home for ALL project memory.** When the owner (or anyone) says
  "**update the project memory**," that means: update the matching `cmem/` topic file with the
  latest decisions, found bugs, design changes, and current state — then add/refresh its one-line
  pointer in the table below. Convert relative dates to absolute; update existing entries rather
  than duplicating.
- **`VISION.md` and `wasmtk-stdlib-bundling-brief.md` were merged into `cmem/`** (now `vision.md`
  and `stdlib-bundling-brief.md`, 2026-05-31) so nothing authoritative lives outside this folder.
- **`README.md` is NOT project memory.** It is the public, user-facing document shipped to GitHub
  and JSR — a concise-but-complete guide to *using* the wasmtk module system (what it is, install,
  per-tool usage, capability surface, worked examples). Keep it as descriptive/verbose as a user
  needs to get full benefit; do NOT mix internal decision logs / bug post-mortems into it.
- The legacy `CLAUDE.md` (repo root, gitignored, machine-local) is the auto-loaded historical
  archive only; `cmem/` supersedes it as the source of truth.

### The "update the project memory" trigger (binding on every agent)

When the owner says **"update the project memory"** (or any clear synonym — "update memory",
"record this", "remember this for the project"), the required action is BOTH of:

1. **Revise all relevant `cmem/` files** — fold the latest decisions, found bugs, design changes,
   and current state into the matching topic file(s); refresh the one-line pointer in the Files
   table; convert relative dates to absolute; update existing entries instead of duplicating.
2. **Sync `README.md` where, and only where, the change is user-relevant** — i.e. update the
   user-facing capability surface, install/usage, examples, and status so the README *matches* the
   new reality. Keep README user-facing: do NOT copy internal decision logs / bug post-mortems into
   it (those live in `cmem/` only). README "matches" the memory on the user-visible facts, not by
   absorbing the internal detail.

This is the durable contract for this repo. Any agent reading this file is expected to honor it.

### The "look for code issues" trigger (binding on every agent)

When the owner says **"look for code issues"** (or a clear synonym — "code audit", "audit the code",
"hunt for bugs"), perform a **comprehensive code audit** for things that may cause future problems —
covering **both tested AND untested code paths**:

1. **Workarounds / temporary hacks** — `TODO`/`FIXME`/`HACK`, version-gated shims, anything described
   as a stopgap; verify still-needed vs. stale/removable.
2. **Dead code** — unused private methods / module functions / fields / flags, duplicate helpers,
   unreachable branches, orphaned exports. Verify each with a grep before reporting.
3. **Bugs** — silently-wrong codegen, inverted logic, type-inference gaps, off-by-one / scanner bugs
   (esp. operator/bracket scanners that skip the string tail — see the `findBinaryOp` /
   `findTopLevelOp` invariants in `design-decisions.md`).
4. **Fall-throughs** — the compiler's worst failure mode: an unhandled input emitting a comment-stub
   + a bare `0` / empty string instead of erroring. Enumerate every terminal "give-up" path and
   assess whether a plausible valid input could reach it and be silently miscompiled; prefer
   converting silent-wrong to a hard `diagnostics` error (guarding speculative probes with
   `quietEmit`), and beware false positives from benign multi-line-construct fragments.

Fan out parallel read-only investigators per category when the surface is large (`src/wasic.ts` is
~16k lines). Report findings with `file:line` + severity; then triage and fix the safe/clear ones,
validating the full suite stays green (output-diff, not just exit codes — see `testing.md`). The
goal is durability: catch issues that won't surface in today's tests but will bite a future change.

This is a durable contract for this repo. (If you prefer a less ambiguous keyword than "look for code
issues", "code audit" / "audit the code" work identically.)

## Files

| File | What it holds |
| --- | --- |
| [overview.md](overview.md) | What wasmtk is, repo layout, the key source files |
| [architecture.md](architecture.md) | wasic / modc / bindgen / hybrid; pluggable wabt+binaryen backends; build & merge pipeline; Canonical ABI **calling-convention alignment** (param + **return side forward-aligned 2026-06-15**: callee-allocated i32-ptr returns + `cabi_post_<name>`; only the P2 container deferred) |
| [polyglot-producers.md](polyglot-producers.md) | **Goal — one congruent polyglot wasm capability** (TS/JS+Rust+Zig+Go producers → shared P1-core/bindgen/binaryen-ts/wasmmerge/host back end); WASI-P1 scope pin; **VERIFIED 2026-06-03 wasmtk is NOT a P2 producer**; **DECISION: forward-align the ABI to canonical while staying P1** (**return side IMPLEMENTED 2026-06-15** — callee-allocated i32-ptr returns + `cabi_post_<name>`; bindgen 104/104); ADR — Go via TinyGo. **Go producer v1 SHIPPED 2026-06-06, refined 2026-06-07** (`src/gowasic.ts`) — via `--lang=go`: `init`(scaffold a **wasm library** by default; `--go-target=wasm`=browser), `modc`(**WASI reactor library** by default; `--go-target=wasm` for browser+`wasm_exec.js`), `run`(build wasip1 command + run; auto-detects Go); TinyGo/std; **`wasic --lang=go` REMOVED 2026-06-07** (standalone Go→WASI isn't merge/bindgen-consumable; the wasip1 build stays inside `run`, which needs it; `wasic` case now prints a pointer to `run --lang=go`); **`run` auto-detects Go 2026-06-07** (`.go` file OR dir-with-`go.mod` → builds+runs without `--lang=go`; detection in `main.ts isGoRunTarget()`, not gowasic, so non-Go `run` skips loading binaryen; flag still optional-override for bare cwd); **`init` dir-creation bug FIXED 2026-06-07** (`scaffoldGoProject` now `rt.mkdir`s the target before `go mod init`); **Go library-compile GAP CLOSED 2026-06-07** — `modc --lang=go` now builds a **WASI reactor library** by default (`-buildmode=c-shared`: no `_start`, exports `//go:wasmexport` funcs, callable via `wasmtk mod`/bindgen — the Go analog of TS `modc`); **browser build moved behind `--go-target=wasm`**; required a **`_initialize` fix** in `wasmtk mod`/`run` (reactor exports trap unless `_initialize` runs first) + an actionable `syscall/js`-in-library-build hint. Verified `modc --lang=go` lib → `wasmtk mod lib.wasm add 2 3` → 5. (Reactor is the standalone/bindgen heap shape — `memory.grow`; a *mergeable* Go leaf still needs alloc-free `wasm-unknown`, not auto-wired.) The root-`main.go`+subfolder test loop runs via `run`; wasm-opt auto-fallback = passthrough shim + `-scheduler=none` + binaryen-ts `-Oz` (goroutine-free, no external binaryen); goroutine Go needs real wasm-opt (asyncify). Deferred: Go string/aggregate **bindgen** only (the reactor/library is DONE — see GAP CLOSED above). **VERIFIED 2026-06-07 native-producer mergeability:** pure libraries of exported functions from Go/Rust/Zig are mergeable; **allocation is the gate** (not language/style) — allocating code merges only for **Zig + static arena (`FixedBufferAllocator`)**; Go allocating **silently corrupts** (runtime allocator, hardcoded addrs + `memory.grow`, slipped the `call_indirect` guard → now CLOSED by a `memory.grow` merge guard, 2026-06-07), Rust allocating caught loudly; true shared-heap unification tractable for Rust/Zig (point allocator at imported `$__malloc`), not Go. **`init` defaults to a wasm LIBRARY scaffold 2026-06-07** (`GoScaffold` `library`|`browser`; no flag needed — the Go producer's primary output is a library; `main.go` has a `//go:wasmexport` example + a test `main`; post-init prints Test=`run` + Build=`modc`; `--go-target=wasm` for a browser project). **Single-file lib+tests pattern verified 2026-06-07**: one `main.go` with `//go:wasmexport` funcs + a `func main` test harness → `wasmtk run` runs the tests; a reactor/library build (`-buildmode=c-shared`) DCE-strips the harness and exports only the marked funcs (TinyGo guarantee). Non-main library code is statically linked into the wasm (package boundary flattened). **Zig producer SHIPPED 2026-06-07** (`src/zigwasic.ts`, `--lang=zig`): init(library scaffold)/modc(freestanding library via `--export=<name>` scan + binaryen-ts `-Oz`)/run(wasm32-wasi on TS host); scaffold `main` comptime-guarded (Zig analyzes `main` even with `-fno-entry`). **Rust producer SHIPPED 2026-06-07** (`src/rustwasic.ts`, `--lang=rust`): **delegates fully to `rsxtk`** — init/initmod/modc(→build wasm)/build(→build wasi)/run/add/remove/list/fmt/clean; prereq `rustup target add wasm32-wasip1`; rsxtk-side gap: `initmod` library build expects `main` (fix in rsxtk). Host change: **WASI-import Proxy** (`makeWasiImport`) stubs unimplemented wasi fns so Zig/Rust std's fuller surface instantiates. `binaryenOptimize` moved to `src/binaryen.ts` (shared Go+Zig) |
| [capabilities.md](capabilities.md) | Stage 0.7 Tier-1 stdlib capability libraries: Set, Map, Date, JSON, RegExp (shared-heap / leaf via wasmbundle); + virtual `wasmtk:<cap>` imports & feature-level tree-shake (brief §7-#4) |
| [compiler-bugs.md](compiler-bugs.md) | Live bug log — **NO OPEN SUITE-FAILING BUGS** (suite **336/336**, bindgen **122/122**, jstyper 73/73 as of 2026-06-24). **Post-v1.9.0 "look for code issues" audit of the functions-as-`any` work (2026-06-24):** 3 real fixes — bindgen tag-7 proxy double-unpin (ABA→UAF: unregister token + idempotent release); tsbundler auto-merge trigger sniffed a fragile regex strip that ate `//` inside strings (→ single-pass `stripCommentsAndStrings` scanner); wasic `eval(`/`Function(` pre-passes rewrote inside string literals + comments (→ `rewriteOutsideStringsAndComments` code-only scanner; regression `18zb`). **Fix (13.4b):** console.log/error `arr[idx].field` struct-lookup now returns ptr+len for a STRING field (was ptr-only → printed raw ptr); surfaced by `Promise.allSettled`'s `results[i].status`. **Latent fix (13.1b):** expression-body arrow result-type inference now seeds the enclosing fn's params/locals, so a CAPTURED var (`v => base + v`) is typed (was → f64) — surfaced by capturing-closure `.then` callbacks. **Pre-publish hardening (2026-06-12):** terminal emit "give-up" fallbacks now record a diagnostic (abort) instead of silently emitting `0`/`""` (guarded probes use `quietEmit`); this surfaced + FIXED a real latent bug — brace-less AND single-line-braced `else`/`else if` after a single-line `if` were silently DROPPED (`emitBlock`; regression `15_ElseChainForms`); plus `instanceof <built-in>` (Error family→1) and dead-code removal (`allocStringNoLog`, orphaned `runner.ts`/`args.ts`/`checkIsLibrary` + their `deno.json` exports). **console.log comparison fixes (2026-06-12):** `findTopLevelOp` paren-tail bug (RHS ending in `)` hid the operator → silent-wrong); string ===/!== fn/field/slice/method operands (compared vs `""`); string `!==` inversion; string `.length` as an operand; member/element-target chained assignment (`p.x = z = 5`); regression `27_ConsoleLogStringCompare`. Pre-bump audit + remaining-items pass (2026-06-08/09) fixed: greedy method/`new` in binary ops (`a.m()+b.m()`, `new A()+new B()`), console.log array-element + i32-global arithmetic, chained `new X().method()`, runtime `n.toString(radix)`, module-level multi-statement lines, string-method-on-array-elem assignment, and the **wasmmerge `relocateDataPtrs`** arithmetic-constant over-relocation (now skips pure-arith-op operands; regression `18i`). **The 14 output-mismatch bugs the 2026-06-07 runner-hardening surfaced are ALL FIXED 2026-06-08**, plus a follow-up hazard audit fixed 4 latent issues (brace-less for…of, console.error bool formatting, class `ptr<0` sentinel, extended greedy-regex guards). (console.log `arr[i].method()`/string-methods/string-literal-concat; for-of static `+8`; struct-field `++`/`+=`; nested-string-struct-field + struct-redecl scope; heap-struct `ptr<0` base; module mutable string globals; closure/funcref string returns; `instanceof Error` catch-ternary; **binaryen `-Oz` skips exception modules** (CoalesceLocals miscompiles catch-var locals) + terminal-`block` fallthru `(unreachable)`; 27_string-formatting char-subscript/self-ref-concat/single-line-while/split-on-template/padStart-in-templates/toString-radix). Earlier: Phase 51 `instanceof` + 3 class gaps + `call_indirect`/`memory.grow` merge guards + reactor `_initialize` fix; 7 long-standing failures fixed 2026-06-02 |
| [design-decisions.md](design-decisions.md) | Load-bearing invariants and codegen rules that must not be silently reverted — incl. **Async/Promise (#13) invariants (2026-06-15): runtime is INLINE not a merged capability (call_indirect guard), canonical `result<T,E>` promise layout, `isPromiseExpr` recurses on the `.then` receiver, `parenDepthNeverNegative` guards on greedy resolve/then regexes, forced reaction-functype+funcref-table when `needsPromiseRuntime`, rejection gated by `needsPromiseReject`** + **Go producer CLI/build invariants** (2026-06-07: `wasic --lang=go` removed; `modc`=WASI reactor library default, browser via `--go-target=wasm`; `run`=wasip1 command + Go auto-detect; `init`=library scaffold; reactor `_initialize` call; `call_indirect`+`memory.grow` merge guards) + **JSR-score invariants (2026-06-15): keep `deno doc --lint` clean** (per-field JSDoc, no `private-type-ref` — `GoResult`/`ZigResult`/`RustResult` stay `export`ed, explicit type on computed consts) **and provenance is environmental** (publish.yml is correct; OIDC token gating is the cause — don't edit the YAML, check org OIDC policy; diagnostic step added) |
| [testing.md](testing.md) | How to run the suites, the test populations, current pass counts, the CI / pre-publish gate (`deno publish` + `deno doc --lint` + fmt/lint/tests; provenance needs the OIDC token to reach the runner), naming conventions |
| [roadmap.md](roadmap.md) | **Release status (2026-06-26): v1.10.8 PUBLISHED to JSR (`latest`) — a big OOP batch (five increments) on the 1.10.x Route A javyc-retirement line: 2e.7a per-iteration `let` (18zj); 2e.7b `var`→`let` consumption gate (`src/varscope.ts`, 18zk/18zl/18zm — ES6 is the base preferred consumption format, the first wasic-compiler change in 2e.x); 2f.1 `this`+prototype (18zn); 2e.8 classes (18zo — also fixed string-blind `parseClasses` via a `maskCode` mask); 2e.8a class COMPLETION (18zp — extends/super/static/fields/getters/setters; class feature complete bar class-expressions). Suite 350/350, bindgen 131/131, jstyper 73/73. Earlier in 1.10.x: 1.10.1–1.10.5 control flow/literals/function-exprs/operators, 1.10.6 exception handling, 1.10.7 lexical block scoping, 1.10.0 bidirectional functions-as-`any`. v1.9.0 (2026-06-24) shipped the COMPLETE #14 own dynamic runtime (value model → interpreter → `any` integration → host↔core marshalling incl. functions → bounded-memory mark-sweep GC → hybrid recycling allocator → functions-as-`any` producer+proxy+pin, end-to-end). v1.8.0 (2026-06-22) shipped the full v1 `async`/Promise surface (#13, 13.1a–13.5). JSR score 100% (provenance `true`, docs clean; keep `deno doc --lint` clean to hold it).** Phase status + **prioritized execution order (set 2026-06-03)**: **Phase 51 language hardening COMPLETE**; **Phase 52 leaf conveniences COMPLETE 2026-06-11** (void / chained assignment / `in` / Array.from-of-isArray / String.fromCodePoint); **Phase 53 standalone built-ins COMPLETE 2026-06-15** (Number.parseInt/parseFloat + bare forms; multi-level interface inheritance, declaration-order independent — suite 309/309); **ABI forward-alignment (return side) COMPLETE 2026-06-15** (canonical callee-allocated string returns + `cabi_post_<name>`); **#12 polyglot loaders substantially DONE — updated 2026-06-22** — `-js` PUBLISHED (`@jrmarcum/universal-wasm-loader@1.0.8`, JSR score 100, provenance true); **all 10 loader ports now implemented on SPEC-3.0.0 — no stubs remain** (`-dotnet` is now a real Wasmtime impl, commit `98dcf2f`; `-go` on wazero, `c7d9bb0`); **#12 COMPLETE 2026-06-24 — ALL 10 ports PUBLISHED** (`-js`→JSR, `-v`→VPM, `-zig`→zigistry, `-py`→PyPI, `-rs`→crates.io, `-go`→pkg.go.dev, `-dart`→pub.dev, `-dotnet`→NuGet, `-jvm`→Maven Central, `-c`→vcpkg; see vision.md). **#14 own runtime COMPLETE + PUBLISHED (v1.9.0 + Phase 2 in v1.10.0).** Remaining = the deferred P2 container (browser-native WASI P2 gating), Go bindgen aggregate marshalling, binaryen-ts asyncify, optional utility-types, optional #12 SPEC §10 loader-caps, and the in-progress Route A (javyc-retirement dynrt 2e/2f growth). Links to the two long-form docs below |
| [vision.md](vision.md) | **Full** polyglot ecosystem vision + "TypeScript as a DLL" vision: guiding decisions, layer diagram, language-support matrix, staged roadmap, repo map. **Loader runtime + WASI strategy (decided 2026-06-15):** native/server ports use **wasmtime** (`-rs` crate, `-py` wasmtime-py, `-go` **wasmtime-go** over wazero for speed, `-c`/Zig wasmtime C API, `-dotnet` Wasmtime NuGet) with built-in WASI; web/browser ports (`-js`, `-dart`-web) use the host `WebAssembly` + a hand-rolled WASI-P1 shim; `-jvm` keeps **Chicory** (no wasmtime JVM embedding) + `chicory-wasi`; `-dart` is dual-backend (web js_interop now / native `dart:ffi`→wasmtime later) + per-language publishing matrix. |
| [dynrt-design.md](dynrt-design.md) | **roadmap #14 own dynamic runtime** — design + log. Locked decisions (owner 2026-06-22): value+object model FIRST; **tagged heap cell (i32 handle)**; authored in the **wasic TS subset now** (zero dup), extract a shared `rtcore`+hand-WAT only at the interpreter increment; **runtime-only first** (no wasic `any` yet). **Increment 1 (value+object model) SHIPPED 2026-06-22** as a shared-heap `modc` cap: boxed value = 4-slot `Int32Array` node `[tag,a,b,c]` (undefined/null/bool/number-f64/string/array/object), self-managed `Int32Array` growable lists for containers, ~23 exports incl. `dynTypeof`/`dynStrictEq`(===)/`dynAdd`(+); library+driver+`18j` pipeline test PASS. Surfaced 3 wasic gaps (str-concat-of-two-calls, Float64Array-elem compare mis-infers i32, **empty-`[]` is a shared static cap-0 array + cap-0 grow broken**) — worked around in the lib, logged in compiler-bugs.md. **1b — virtual `wasmtk:dynrt` import + tree-shake — SHIPPED 2026-06-22** (embedded in `caps_bytes.ts` as a 6th registry entry; test `18k`). **2a — `eval` of a pure expression language — SHIPPED 2026-06-22** (recursive-descent direct-eval parser in the subset — decision: continue in the subset, no `rtcore`/hand-WAT needed yet; full operator precedence + parens + unary + ternary + string concat; added dynamic ops `dynSub/Mul/Div/Mod/Neg/Not/Lt/Gt/Le/Ge`; no new compiler gaps; test `18l`). **2b — variables + environment + member/index access — SHIPPED 2026-06-22** (`dynEvalEnv(s,env)`; identifiers resolve in an env object; `.prop`/`["key"]`/`[i]`/`.length`; TOTAL/guarded member access; no new compiler gaps; test `18m`. Scope split: calls + real short-circuit moved to 2c — short-circuit is only observable with side-effecting calls). **2c — function values (tag 7) + calls + REAL short-circuit — SHIPPED 2026-06-22** (built-ins via static-switch dispatch — no `call_indirect`, merge-safe; `dynStdEnv()` abs/sqrt/…/inc; `evalLive` skip-parse short-circuit guarding the call dispatch, proven via the `inc()` counter; surfaced 1 wasic gap — i32 global/typed-array-elem as f64 arg skips f64.convert, worked around; test `18n`). **2d.1 — statements + control flow (`dynRun`) — SHIPPED 2026-06-22** (let/const/assignment, if/else, while, blocks, return; direct-eval re-parse loops; mutable env via dynSet; ran factorial/fibonacci/nested-loops/early-return; STILL in the subset — no wall, no rtcore/hand-WAT needed; no new compiler gaps; test `18o`). **2d.2 — user-defined functions + `new Function` — SHIPPED 2026-06-22 (COMPLETES interpreter increment 2)** (5-slot user-function cell holding body+params+defining-env; in-source `function` decls + `dynMakeFunc`=new-Function-from-strings; recursion+closures via an `envLookup` scope chain; parser-reentrancy save/restore around the nested `dynRun`; still in the subset — no `rtcore`/hand-WAT needed; known heap-bound-recursion limit, no GC — `fib(8)` used; test `18p`). **#14 interpreter (2a–2d.2) DONE — the §6 eval/`new Function` kernel is covered in the subset.** **Increment 3 STARTED (the first wasic-COMPILER change): 3.1 — wasic `any` type + auto-merge — SHIPPED 2026-06-22** (`mapType("any")→i32` boxed handle; bundler auto-injects `wasmtk:dynrt` on `any`/`eval` usage; implicit boxing of literal `: any =` initialisers; `as`-unboxing via an `anyVars` side-set; new dynrt export `dynStrBytes`; key wiring lesson: compiler-introduced calls need the merged `dynrt_` prefix; test `18q`). **3.2 — operators on `any` — SHIPPED 2026-06-22** (guarded block in the binary-op loop fires ONLY when an operand is an `any` var, else existing typed paths untouched; `boxAnyOperand` helper; arithmetic→`dynrt_dynAdd/…` handle, comparisons→raw i32, `&&`/`||`→truthiness; suite green — zero impact on non-`any` code; `18q`). **3.3 — member/index/call on `any` + bare `eval` — SHIPPED 2026-06-22** (guarded any-dispatch in emitExpr: `x.foo`→`dynrt_dynMember`, `x[i]`→`dynrt_dynIndexValue`, `x(args)`→`dynrt_dynCall0/1/2/3`; library now EXPORTS `dynMember`/`dynIndexValue` + new `dynCall0-3`; `eval(`→`dynrt_dynEval(` pre-pass; `18q`). **3.4 — hybrid `--auto` migration — SHIPPED 2026-06-22 (COMPLETES increment 3 + the #14 arc)**: functions with a TYPED signature + DYNAMIC (`any`/`eval`) body now route to the wasic+dynrt core (typed sig marshals normally); try-compile / fall-back-to-host ladder (`runHybrid` detects failure by whether the core `.wit` was produced, since modc doesn't throw); also fixed `compileLibTs` (modc) to handle embedded virtual-cap `entry.bytes`/`witText` like `compileWasiTs` (auto-merge now works in modc); fixtures `dynamic_hybrid`/`dynamic_fallback_hybrid` verified. **`hybrid --auto`'s dynamic target is now the own runtime, not javyc.** **#14 FOLLOW-UP — any-signature host↔core marshalling — SHIPPED 2026-06-22**: a function with an `any` param/return is callable from the host with real JS values (number/string/bool). 4 parts: track `any` sigs (`FuncParam.isAny`/`FuncDef.isAnyResult`, + any-PARAMS now in `anyVars`); `generateWit` emits `any`; `injectDynrtMarshalExports` exports the dynrt box/unbox helpers post-merge (both compile paths); bindgen `_box`/`_unbox` (WitType `any`). Verified `tests/wasm_wasi_bundle/anysig_bundle/` (identity/addOne/typeName/exclaim round-trip number/string/bool); bindgen 104/104. **#14 FOLLOW-UP — hybrid fallback refinement — SHIPPED 2026-06-22**: per-function fallback (was all-or-nothing) — on core-compile failure, probe each dynamic fn against the static context and move ONLY the failing ones to host (`parseHybridFile` `excludeFns`; `probeCompiles` to a cleaned-up `_probe.*` module); safety ladders preserved. Verified `dynamic_partial_hybrid` (good dynamic stays in core, bad → host). **#14 FOLLOW-UP — objects/arrays as `any` STRUCTURAL marshalling — SHIPPED 2026-06-22**: objects/arrays cross the host boundary as real recursively-converted JS objects/arrays (was opaque handles) — dynrt gained `dynObjKeyPtr/Len`+`dynObjValAt` exports, the marshal-export + auto-import lists gained the container accessors, and bindgen `_box`/`_unbox` recurse on `dynTag` (5=array/6=object). Verified `anysig_bundle` (`makePoint`→`{x,y}`, `triple`→`[…]`, `sumArr([…])`, `getX({…})`). **#14 MEMORY/GC track — building a full mark-sweep GC in TESTED PARTS (owner 2026-06-22). Part 1 — auto-grow `$__malloc` — SHIPPED 2026-06-22** (`memory.grow` by ceil(deficit/64KiB) pages when the bump ptr runs past the allocated pages; lifts the fixed ~2-page limit → `fib(15)`≈1973 interpreter calls now runs, was overflowing at `fib(10)`; gated to executable/WASI modules only — a merged lib's malloc is dropped+replaced and `memory.grow` defeats `detectBumpAllocator`/wabt-ts merge; test `18r`). **Part 2 — free-list allocator — SHIPPED 2026-06-22** (`$__free(ptr,size)` links blocks ≥8B into a `$__free_list`, storing [size@0,next@4] in the block; `$__malloc` first-fits before bumping; no splitting v1; executable-only; dormant until the GC sweep calls free in P5; tested via new `__malloc`/`__free`/`__heapPtr` intrinsics, test `18s`). **Part 3 — cell registry — SHIPPED 2026-06-22** (every value cell flows through `mkCell`/`mkCell5` → recorded in a registry list `__gc_reg` so P4/P5 can enumerate all allocations; `dynGcCellCount()` hook; append inlined to limit recursion-depth cost; test `18t` tracks 20004 cells. SURFACED+FIXED a latent wasmmerge bug: it clobbered ALL merged mutable globals to 131072 — fine for write-before-read state but it destroyed `__gc_reg`'s 0-sentinel → heap corruption/OOB at ~3-4k cells; fixed by gating the 131072 rewrite to the non-allocator-unified case only — see compiler-bugs.md). **Part 4a — mark phase — SHIPPED 2026-06-22** (`gcMark(root)` recursively marks every reachable cell; mark bit = tag bit 8, uniform/no-extra-storage, doubles as visited-set so cycles terminate, cleared by collect() so the runtime never sees a polluted tag; follows array/object element handles + user-fn body/params/env; exports `dynGcMarkClear`/`dynGcMark`/`dynGcMarkedCount`; test `18u` verifies reachability across nesting + orphan exclusion). **Part 4 root strategy DECIDED: interpreter SHADOW-STACK** (precise, safe mid-interpretation; won't collect `any` handles in arbitrary wasic locals — documented scope). **Part 4b — shadow-stack roots — SHIPPED 2026-06-23** (`dynRun` pushes/pops its scope on `__gc_roots`; `dynGcMarkRoots` marks from all roots; exports `dynGcPushRoot`/`dynGcPopRoot`/`dynGcRootCount`; companion fix: `gcMark` now follows an env's parent link in object slot 2 + guards the -1 no-env sentinel, so marking one scope keeps the whole lexical chain + closure captures; test `18v` — balance + driver-root marking + closure-chain). **Part 5a — mark-sweep collect (cells) — SHIPPED 2026-06-23** (`dynGcCollect()` = mark-roots → sweep registry → reclaim unmarked cells + compact; dynrt now allocates cells via its OWN recycling free list `dynAlloc`/`dynFreeBlock`/`__dyn_free` — reused blocks zeroed — so the GC reclaims into the same pool, NO wasmmerge `$__free` unification needed; `dynGcMarkRoots` also marks interpreter regs evalEnv/last/return; exports `dynGcCollect`/`dynGcFreeCount`; test `18w` — reclaim + compact + live-survives + reuse). **Part 5b — payloads + auto-collect — SHIPPED 2026-06-23; GC TRACK COMPLETE** (constructors' Float64Array/Uint8Array/list allocs now via `dynAlloc`, sweep frees payloads by tag via `gcFreePayload`; `maybeCollect` auto-collects at interpreter statement boundaries past an adaptive threshold; the recursive-descent evaluator now ROOTS mid-expression intermediates — `left` across each right-operand parse at every binary level + callee/args across a call — else `fib(n-1)+fib(n-2)` corrupts; gotcha: that wrapping dropped the `&&`/`||` `evalLive=saved` restore, fixed. Test `18x`: a 10000-iter loop allocates ~30000 cells but runs in BOUNDED memory, sum correct, 5 live cells after a final collect). **#14 GC done (P1–P5b): dynrt runs long-lived dynamic code in bounded memory.** **GC polish SHIPPED 2026-06-23:** both leaks FIXED — `dynAlloc`/`dynFreeBlock` round to `GC_MIN_BLOCK=16` so <16B short-string/key payloads recycle (the only growing leak), and `mkCell` frees the old registry array on grow. **free-list SPLITTING also SHIPPED 2026-06-23** (test `18y`): `dynAlloc` now carves the leftover (≥16B) off an oversized reused block at `cur+size` instead of losing it on the next free (a slow size-mismatch leak; only bit mixed-size workloads — number-heavy code already did exact reuse). **Functions-as-`any` — Phase 1 SHIPPED 2026-06-23 (the #14 final item):** the host-PIN-table that was the missing prerequisite is built — `dynGcPin(h)`/`dynGcUnpin(slot)` record a handle in a slot list `dynGcMarkRoots` also marks (non-moving GC → pinning just keeps it marked), so a function returned to the host survives collections; bindgen `_unbox` tag-7 returns a JS proxy that pins, calls back via `dynApply`, and `.release()`s (+ FinalizationRegistry); `dynApply`/`dynGcPin`/`dynGcUnpin` added to the marshal-export set; tests `18za` (pin runtime round-trip) + bindgen `testGenBindingsAnyFunction`. **PRODUCER + FULL END-TO-END SHIPPED 2026-06-23:** `Function(params, body)` (and `new Function`) lowers (wasic.ts pre-pass) to a new dynrt export `dynMakeFn` (auto-merge trigger `usesFunction` + auto-import added); a modc lib `getDoubler(): any { return Function("x","return x*2;"); }` → bindgen → host `m.getDoubler()(21)` → **42** (bindgen `testIntegrationFnAny`, 119/119). Gotcha fixed: `usesFunction` matched `Function(` in dynrt's own COMMENT → lib self-merged (circular) → now strip comments before sniffing triggers. **Functions-as-`any` core→host is DONE end-to-end.** Phase 2 (host→core via imported `__hostcall`) still deferred. **Adjacency-coalescing SHIPPED 2026-06-23 as a HYBRID allocator (test `18z`)** — coalesce-on-free hung (a re-entrancy cycle: dynAlloc splits + frees mid-allocation), so re-done as: segregated buckets (16/24/28/32, O(1), can't cycle) + Tier-2 general (first-fit+split) + a BATCH `defragFull` (snapshot gather→address-mergesort→merge-adjacent→redistribute; no re-entrancy) triggered proactively (adaptive node threshold, amortized) + on-demand (free-bytes≥request). Verified via a `dynGcCheckHeap` integrity hook through the exact stress that hung the first attempt. Balance: hot path never defrags, large/odd sizes get coalescing efficiency. See dynrt-design.md. **#14 COMPLETE 2026-06-24, NO deferred pieces (v1.9.0 + Phase 2). Phase 2 host→core callbacks SHIPPED 2026-06-24** — a JS fn passed INTO the core as `any` is called back via an `env.__host_call` import (bindgen host-fn table + `dynMakeHostFn`); `applyTwice(n=>n+1,10)=12`/`combine=42` (bindgen `testIntegrationHostFn`). Surfaced+fixed a wasmmerge bug (non-WASI imports spliced after func defs → OOB; now routed to the import section — see compiler-bugs.md). functions-as-`any` is now BIDIRECTIONAL. `dynrt` is the PRIMARY dynamic engine, `javyc` the fallback. The javyc-retirement track is now FULLY SCOPED in dynrt-design.md — `javyc` is a thin external-Javy/QuickJS wrapper whose only wiring is the standalone `wasmtk javyc` command (hybrid uses dynrt+host); Route A = grow dynrt via 2e (language) + 2f (stdlib, several bridge existing cap libs) + 2h (full-dynamic-compile entry + Javy-parity gate + delete src/javyc.ts), Route B = policy-drop in one PR; 2g GC already done. **ROUTE A CHOSEN + IN PROGRESS (owner 2026-06-24): 2e.1 control flow (for/for-of/for-in/switch/do-while/break/continue/++/+=, test 18zc), 2e.2 literals (array/object/template, 18zd), 2e.3 functions (function-exprs + arrows + closures, 18ze), 2e.4 member/index assignment (`o.x=v`/`arr[i]=v`/compound/nested/computed, 18zf), 2e.5 operators (`typeof`/`??`/`?.` optional-chaining/array-spread, 18zg), 2e.6 try/catch/throw/finally (18zh — `evalThrew`/`evalThrowVal` control globals mirror `evalReturned` but are NOT consumed at the call boundary, so a throw unwinds through statements/loops/CALLS until `runTry` catches; binding + binding-less `catch {}` + `try/finally`; purely interpreter-side, no wasic change), 2e.7 lexical block scoping (18zi — each `{ }`/`for`/`catch` runs in a fresh `childEnv`; `let`/`const` don't leak; bare assign/`++`/`+=` walk the chain via `envAssign` to update the declaring scope, not a shadow; fresh `catch (e)` scope), 2e.7a per-iteration `let` (18zj — loop-closure capture; `var` stays shared), 2e.7b `var`→`let` consumption gate (`src/varscope.ts`, 18zk/18zl/18zm + varscope_tests 12/12 — the FIRST wasic-compiler change in 2e.x; auto-repair provably-safe `var`→`let`, hard-error unsafe ones, code-only via maskCode) 2f.1 `this` + prototype (18zn — object-literal method shorthand, `obj.m()` binds `this`, `this.field` r/w, `Object.create(proto)` + prototype-chain lookup), 2e.8 classes (18zo — `class Name { constructor(){…} method(){…} }` + `new Name(args)`; `runClassDecl`+`dynNew`+`new` operator on the 2f.1 foundation; also fixed a general wasic bug — `parseClasses` was string-blind, now uses a `maskCode` code-only mask), 2e.8a class COMPLETION (18zp — `extends`+`super`+`static`+instance-fields+getters/setters; class feature complete bar class-expressions), 2f.2 stdlib Array methods (18zq — push/indexOf/includes/join/slice/concat/reverse + map/filter/forEach/reduce; `dynArrayMethod` dispatched in parsePostfix) all SHIPPED (v1.10.1–v1.10.8 + 2f.2 unreleased, suite 351/351). Fixed 3 general wasic bugs — `=>` AND `?.` detection mis-firing inside string literals (string-blind pre-passes → now code-only via `rewriteOutsideStringsAndComments`); and a latent process bug shipped in v1.10.6 SOURCE — `deno fmt` had wrapped 3 deeply-indented dynrt ternaries into multi-line form modc can't parse (binary fine; caps_bytes is pre-fmt) → converted to single-line if/else + lesson: re-run modc on the POST-fmt lib before committing (see compiler-bugs.md). Remaining: 2e.7a per-iteration `let` (loop-closure capture) **SHIPPED 2026-06-26 (18zj — `cloneEnvFlat`+`perIter`; let/const loop vars fresh per iteration, `var` stays shared)**; 2e.7b `var`→`let` safe-autofix / unsafe-hard-error gate **SHIPPED 2026-06-26 (`src/varscope.ts`, wired FIRST in transpile() → covers wasic+modc; zero-risk since codebase is 0-var; follow-up: also scan resolved imports/tsbundler)** (DECIDED 2026-06-26: ES6 is the BASE preferred consumption format; older ES auto-repaired ONLY when provably safe, else hard-error — sound scope analysis required; gate lives at the wasic/tsbundler front-end, JSR ships source≈clean but npm ships var-heavy dist → must scan; see dynrt-design.md "Language consumption profile"), 2e.8 classes (2f.1 prototype/this DONE → now unblocked — SHIPPED 18zo + 2e.8a completion 18zp: extends/super/static/fields/getters/setters), 2f.2 Array methods SHIPPED 18zq → 2f.3+ String/Object/Math methods + JSON/Map/Set/RegExp bridges, destructuring-assign, `instanceof`/object-spread/call-spread, 2e.9 generators, 2e.10 async, 2h removal.** |
| [async-design.md](async-design.md) | **roadmap #13 Promise/async — COMPLETE (2026-06-15; suite 309→317, zero regressions).** All sub-phases 13.1a + 13.2 + 13.3a + 13.3b + 13.1b + 13.4 (all + allSettled) + 13.5 (hybrid async lift) shipped — the entire v1 Promise API surface (async/await + resolve/reject + then/catch/finally + all/allSettled, standalone WASI) plus hybrid integration. **13.1a:** `async`/`await` + `Promise.resolve`, async-fn-returns-promise (i32/f64), inline runtime, promise object in canonical `result<T,E>` (test `54_AsyncBasic`). **13.2:** `.then(namedCb)` + microtask queue (FIFO linked list) + drain via per-call-site `call_indirect` trampolines; correct ordering/FIFO/chained/f64/await-of-then (test `55_AsyncThen`). **13.3a:** `Promise.reject` + rejection→exception (rejected `await` re-throws, caught by `try/catch`) + async-body-throw caught free via eager model (test `56_AsyncReject`). **13.3b:** `.catch`/`.finally` rejection reactions + `.then(onF,onR)` via a **dual-path** trampoline (`genReactionTrampoline` branches on `src.disc`: fulfilled→passthrough/`onF`, rejected→`onR(reason:string)`/propagate; `.finally` both paths) (test `57_AsyncCatch`). **13.1b:** promise-holding-var inner-type tracking (`const p = f(); await p`/`p.then`, i32/f64/aliasing — test `58_AsyncPromiseVar`) + **capturing-closure callbacks** for `.then`/`.finally` via an **env-bearing reaction record** (functype → `(env,src,result)`; closure dispatched by `call_indirect` through the closure ptr — test `59_AsyncClosureCb`). **13.4:** **`Promise.all` + `Promise.allSettled`** (array-literal arg, i32/f64) via per-call-site combinators — `all` drains+builds `T[]`/first-rejection-wins (test `60_AsyncAll`); `allSettled` never rejects, builds a synth-`__settled_<T>` struct array of `{status,value,reason}` (test `61_AsyncAllSettled`). **13.5:** lift the `hybrid` async exclusion (`src/hybrid.ts`) — route async fns into the wasic core via an internal `f__impl` + a sync unwrapping wrapper `f` (`return await f__impl`), validated by `tests/hybrid_fixtures/async_hybrid.ts`. **#13 async track COMPLETE.** WASI-P1 has no event loop → async = deterministic deferral, not concurrency (true suspension needs stack-switching; no cross-language async ABI until WASI-P3 → v1 async is intra-module only). **Decided:** Approach A (microtask-drain) for v1, expand to B (state-machine CPS) later; value slot = type-tagged union **+ settled value laid out as Canonical ABI `result<T,E>`** + opaque handle (max forward-compat, future P3 lift = wrap); runtime = **inline WAT helpers** (`getPromiseRuntimeWat()` + `needsPromiseRuntime`), NOT a merged capability — the `wasmmerge` `call_indirect` guard forbids a callback-bearing merged module; locked "never introspects callbacks" invariant (B drop-in); unhandled rejection → warn-to-stderr; deadlock → trap via mode-scoped `__on_quiescent()` seam. Promise heap layout, queue/drain, trampoline bridge, rejection↔exception integration, 5 sub-phases (13.1–13.5), v1 limits (interleaving order, i32/f64-only `Promise.all`). Scope: async/await + resolve/reject + then/catch/finally + all/allSettled, standalone WASI |
| [stdlib-bundling-brief.md](stdlib-bundling-brief.md) | **Full** on-demand stdlib bundling design brief (§1–§7d): rationale, allocator unification, the two linking regimes, feature tiering, Javy-independence verdict, work items, per-capability + merge-bug post-mortems (moved from root, 2026-05-31) |
| [component-model-discussion.md](component-model-discussion.md) | **DRAFT / OPEN (2026-06-06) — circle back.** Polyglot monorepo component model + language-selection CLI: tentative decisions (producer set = TS/Go/Rust/Zig; explicit `--lang` on init/modc/wasic; auto-detect `run`); the central finding (**cross-language inclusion = `instance`/WIT/bindgen only, never `merge`**); draft `wasmtk.json` schema + validity matrix + build-graph/host-wiring; open decisions. Not implemented |

## Long-form docs are now inside cmem

`vision.md` and `stdlib-bundling-brief.md` were full design documents at the repo root; they were
moved into `cmem/` (2026-05-31) so all project memory has a single home. `roadmap.md` summarizes
both and links to them.

## Related files outside cmem

- `README.md` — the **public, user-facing** doc shipped to GitHub/JSR (install, per-tool usage,
  capability surface, examples). NOT project memory; curated for end users — keep internal
  decision logs / bug post-mortems out of it (those belong here).
- `CLAUDE.md` — legacy exhaustive memory archive (repo root, gitignored, machine-local; auto-loaded
  by Claude Code). Superseded by cmem as the source of truth.
